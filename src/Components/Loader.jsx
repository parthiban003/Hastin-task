import React from 'react';
import './Form.css';

const Loader = () => (
  <div className="loader-overlay">
    <div className="spinner"></div>
    <p>Loading...</p>
  </div>
);

export default Loader;
